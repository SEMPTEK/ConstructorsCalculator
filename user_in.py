# validate and parse values for math using architectural notation
def parse_arch_values(user_data: str) -> list:
    pass


# validate and parse values for math using decimal values
def parse_dec_values(user_data: str) -> list:
    pass


# cleans data to an educated guess at what user wanted to input
def clean_user_data(user_data: str) -> str:
    pass


# convert architectural notation into decimal values to allow for one set of calculations to run for both input types
def arch_to_decimal(user_data) -> list:
    pass
